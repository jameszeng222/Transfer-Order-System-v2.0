# 调拨单管理系统 — 需求文档

> 版本：v1.2 | 日期：2026-05-31 | 状态：确认

---

## 一、项目背景与目标

### 1.1 背景

某跨境电商物流公司，当前调拨单管理现状：

- **国内仓**：4个，出库数据分散在各仓各自的系统中，部分仓库无对接能力，靠Excel管理
- **海外仓**：3个，入库反馈靠邮件/Excel回传，时效性差
- **FBA**：调拨通过亚马逊后台操作，游离在自有管理体系之外
- **ERP系统**：已有，负责调拨单的创建和财务核算，但无法向下追踪执行过程
- **物流在途**：依赖货代反馈，信息分散在邮件和微信中

**核心痛点**：没有一个统一的平台来查看所有调拨单的当前状态、在途货物明细、差异处理情况。

### 1.2 核心目标

> 打造一个以导入为核心的调拨单统一管理平台，让所有调拨数据（从出库到入库到上架）在一个系统中完成状态追踪、时效管理、差异闭环、运费核算和报表分析。

**不做的事**：
- 不替代ERP的调拨创建功能
- 不替代物流商的系统
- FBA（亚马逊）不通过API对接，通过导入方式管理

**API对接策略**：
- **万邑通**：优先对接API，自动抓取上架情况（卸货时间、上架时间、上架数量）
- **其他仓库**（思创南非仓、ONNAT等）：不对接API，通过导入管理
- **FBA**：不通过API对接，通过导入方式管理

**数据流模式**：API自动抓取（万邑通）+ 导入（Excel/CSV）+ 少量手动录入完成数据流转。所有走API创建的和非API创建的调拨单统一汇总到本系统呈现。

---

## 二、系统架构总览

```
┌─────────────────────────────────────────────────────────┐
│                      ERP系统（已有）                       │
│             负责：创建调拨单、财务核算                      │
└──────────────────────┬──────────────────────────────────┘
                       │ 导出调拨单数据 (CSV)
                       ▼
┌─────────────────────────────────────────────────────────┐
│                  调拨单管理系统（新建）                     │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │  数据导入层  │  │  状态引擎   │  │  报表导出   │     │
│  │  ERP导入     │  │  状态流转   │  │  在途报表   │     │
│  │  出库回传    │  │  差异计算   │  │  差异报表   │     │
│  │  入库回传    │  │  在途追踪   │  │  时效报表   │     │
│  │  物流轨迹    │  │  运费分摊   │  │  运费成本   │     │
│  │  运费账单    │  │  异常判断   │  │  在途明细   │     │
│  │  箱规/报关   │  │  时效计算   │  │              │     │
│  └──────┬──────┘  └─────────────┘  └─────────────┘     │
│         │                                               │
│  ┌──────┴──────┐                                        │
│  │  API对接层   │                                        │
│  │  万邑通上架  │                                        │
│  └─────────────┘                                        │
├─────────────────────────────────────────────────────────┤
│                  数据库（调拨单核心数据）                    │
└─────────────────────────────────────────────────────────┘
```

---

## 三、核心数据模型

### 3.0 仓库主表 — warehouse

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| warehouse_code | 字符串(唯一) | 仓库编码（如：GZ、SZ、LA、FBA-US） |
| warehouse_name | 字符串 | 仓库名称（如：广州仓、洛杉矶仓） |
| region | 字符串 | 所在地区（如：国内、北美、欧洲、东南亚） |
| country | 字符串 | 所在国家 |
| timezone | 字符串 | 仓库时区（如：Asia/Shanghai、America/Los_Angeles），用于时间字段自动转换 |
| warehouse_type | 枚举 | 仓库类型：国内仓 / 海外仓 / FBA / 第三方仓 |
| warehouse_category | 枚举 | 仓库分类：自有仓 / 万邑通 / 亚马逊FBA / 思创 / ONNAT / 其他 |
| api_enabled | 布尔 | 是否启用API对接 |
| api_provider | 枚举 | API对接方：万邑通 / 亚马逊 / 无 |
| api_config | JSON | API对接配置（接口地址、认证信息等，加密存储） |
| api_sync_frequency | 字符串 | API同步频率（如：daily、every_4h） |
| last_sync_time | 时间戳 | 最近一次API同步时间 |
| contact_name | 字符串 | 仓库联系人 |
| contact_phone | 字符串 | 仓库联系电话 |
| is_active | 布尔 | 是否启用 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

> 所有涉及仓库的字段（from_warehouse、to_warehouse）均引用 warehouse_code，便于按仓库维度统计和筛选。
> 仓库时区用于将各时间字段自动转换为当地时间显示（如签收日期显示为美国时间）。

### 3.0.1 物流商主表 — logistics_carrier

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| carrier_code | 字符串(唯一) | 物流商编码（如：ZTO-INTL、DHL、FEDEX） |
| carrier_name | 字符串 | 物流商名称（如：中通国际、DHL、FedEx） |
| carrier_type | 枚举 | 物流商类型：国际快递 / 国际海运 / 国际空运 / 铁路 / 卡航 |
| supported_transport_types | 字符串 | 支持的运输类型（逗号分隔，如：海运,空运） |
| supported_routes | 文本 | 支持的路线（如：中国→北美、中国→欧洲） |
| default_currency | 字符串 | 默认计费币种（如：CNY、USD） |
| settlement_cycle | 字符串 | 结算周期（如：月结30天、月结60天） |
| contact_name | 字符串 | 对接联系人 |
| contact_phone | 字符串 | 对接联系电话 |
| is_active | 布尔 | 是否启用 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

### 3.0.2 SLA规则表 — sla_rules

> Phase 1采用简单表结构，按目的仓+运输类型配置SLA天数。Phase 3升级为可配置规则引擎（rule_config），支持异常判断、多条件组合、优先级排序。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| dest_warehouse_id | 整数(外键) | 目的仓ID，关联warehouse表 |
| transport_type | 枚举 | 运输类型：海运 / 空运 / 铁路 / 卡航 |
| sla_days | 整数 | SLA时效天数（收件→签收） |
| shelf_sla_days | 整数 | 上架SLA天数（签收→上架），默认3天 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

> **唯一约束**：(dest_warehouse_id, transport_type)，同一目的仓+运输类型只能有一条规则。
>
> **预置规则**：
> | 目的仓 | 运输类型 | SLA天数 | 上架SLA |
> |---|---|---|---|
> | 洛杉矶仓 | 海运 | 38 | 3 |
> | 洛杉矶仓 | 空运 | 7 | 3 |
> | 洛杉矶仓 | 卡航 | 4 | 3 |
> | US-KY3仓 | 海运 | 38 | 3 |
> | FBA-US | 空运 | 7 | 3 |
> | 东京仓 | 海运 | 15 | 3 |
> | 伦敦仓 | 铁路 | 25 | 3 |
>
> **Phase 3升级路径**：将sla_rules升级为rule_config表，支持：
> - 规则分类（SLA达标 / 超时预警 / 物流异常 / 上架异常）
> - 作用范围（全局 / 按仓库 / 按路线 / 按物流商 / 按运输类型）
> - JSON条件结构（触发事件+指标+运算符+阈值）
> - 优先级排序

### 3.0.3 团队表 — team

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| team_code | 字符串(唯一) | 团队编码（如：NA、EU、APAC） |
| team_name | 字符串 | 团队名称（如：北美组、欧洲组、亚太组） |
| leader | 字符串 | 负责人 |
| is_active | 布尔 | 是否启用 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

> 调拨单主表的 `team` 字段引用 `team_code`。

### 3.0.4 角色表 — role

> Phase 1 建表时建好 RBAC 结构，预设4个角色。Phase 1 只做页面级权限（有权限才看到菜单），Phase 4 细化字段级权限和审批流，数据库结构不用改。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| role_code | 字符串(唯一) | 角色编码（如：ADMIN、OPERATOR、WAREHOUSE、READONLY） |
| role_name | 字符串 | 角色名称（如：管理员、运营、仓库、只读） |
| description | 字符串 | 角色描述 |
| is_active | 布尔 | 是否启用 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

> **预置角色**：
> | role_code | role_name | 说明 |
> |---|---|---|
> | ADMIN | 管理员 | 全部功能，系统设置 |
> | OPERATOR | 运营 | 创建/编辑调拨单，查看在途和报表，导入数据 |
> | WAREHOUSE | 仓库 | 确认发货/到货/上架，处理异常 |
> | READONLY | 只读 | 查看所有数据，不能编辑/导入 |

### 3.0.5 角色权限表 — role_permission

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| role_id | 整数(外键) | 关联角色ID |
| permission_code | 字符串 | 权限码（如：order.import、freight.view），详见11.6权限模型 |
| create_time | 时间戳 | 创建时间 |

> **唯一约束**：(role_id, permission_code)

### 3.0.6 用户表 — user

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| username | 字符串(唯一) | 登录用户名 |
| password_hash | 字符串 | 密码哈希（bcrypt） |
| name | 字符串 | 姓名 |
| phone | 字符串 | 手机号 |
| email | 字符串 | 邮箱 |
| team_id | 整数(外键) | 所属团队ID，关联team表 |
| role_id | 整数(外键) | 关联角色ID，关联role表 |
| is_active | 布尔 | 是否启用 |
| last_login_time | 时间戳 | 最近登录时间 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

> Phase 4细化：字段级权限、操作审批权限、数据范围权限（按团队/仓库）。数据库结构不变，通过 role_permissions 扩展权限码即可。

### 3.0.7 API对接配置表 — api_integration

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| api_code | 字符串(唯一) | API编码（如：WANYITONG_SHELF） |
| api_name | 字符串 | API名称（如：万邑通上架数据同步） |
| api_provider | 枚举 | API提供方：万邑通 / 亚马逊 / 其他 |
| api_type | 枚举 | API类型：数据同步 / 订单创建 / 状态回调 |
| api_endpoint | 字符串 | API接口地址 |
| auth_type | 枚举 | 认证方式：API_KEY / OAUTH2 / BASIC |
| auth_config | JSON | 认证配置（加密存储） |
| sync_frequency | 字符串 | 同步频率（如：daily 08:00、every_4h） |
| sync_direction | 枚举 | 同步方向：拉取 / 推送 / 双向 |
| data_mapping | JSON | 字段映射配置（第三方字段 → 系统字段） |
| last_sync_time | 时间戳 | 最近同步时间 |
| last_sync_status | 枚举 | 最近同步状态：成功 / 失败 / 未执行 |
| last_sync_record_count | 整数 | 最近同步记录数 |
| error_message | 文本 | 最近错误信息 |
| is_active | 布尔 | 是否启用 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

> **万邑通API对接范围**：
> - 数据同步（拉取）：上架数量、卸货时间、上架时间、物流状态
> - 同步频率：每天上午8:00自动拉取
> - 字段映射：万邑通字段 → 系统 transfer_carton / transfer_order_item 字段

### 3.0.8 定时任务队列表 — job_queue

> 用于记录定时任务的每次执行情况，支持重启恢复、失败重试、手动触发。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| job_type | 字符串 | 任务类型（如：winit_sync、sla_check） |
| status | 枚举 | pending / running / success / failed |
| started_at | 时间戳 | 开始执行时间 |
| finished_at | 时间戳 | 执行完成时间 |
| result | JSON | 执行结果（成功时记录同步数量等） |
| error | 文本 | 错误信息（失败时记录） |
| retry_count | 整数 | 重试次数（最多2次） |
| trigger_source | 枚举 | cron（定时触发）/ manual（手动触发） |
| create_time | 时间戳 | 创建时间 |

> **任务类型**：
> | job_type | 说明 | Phase |
> |---|---|---|
> | winit_sync | 万邑通上架数据同步 | Phase 3 |
> | sla_check | SLA超时检查 | Phase 2 |
>
> **运行规则**：
> - 服务器重启时检查 `status = 'running'` 的 job，重置为 pending
> - 手动触发入口：`POST /api/jobs/trigger?type=winit_sync`
> - 失败重试最多 2 次，超过后标记 failed 并记录错误信息

### 3.1 调拨单主表 — transfer_order

> **核心关联键**：`inbound_order_no`（第三方入库单号）是贯穿全系统的中心关联键，用于串联各模块数据。所有导入操作均以 `inbound_order_no` 作为主匹配键，系统内部通过 `transfer_no` 做数据关联。业务人员以"第三方入库单号"为日常沟通标识。

| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| id | 主键 | 系统 | 系统内部ID |
| transfer_no | 字符串(唯一) | 系统 | 调拨单号（系统内部主键，全局唯一，如 TO-20260523-0001） |
| erp_order_no | 字符串 | 系统映射 | ERP侧单号，用于关联回查（已废弃，删除） |
| outbound_order_no | 字符串 | 系统映射 | 发货仓出库单号 |
| inbound_order_no | 字符串(唯一) | 系统映射 | **第三方入库单号（核心关联键）**，目的仓（海外仓）的入库单号，贯穿全链路 |
| from_warehouse | 字符串 | 系统映射 | 来源仓（引用 warehouse_code） |
| to_warehouse | 字符串 | 系统映射 | 目的仓（引用 warehouse_code） |
| team | 字符串 | 系统映射 | 业务团队 |
| source | 枚举 | 系统映射 | 数据来源：万邑通API / 亚马逊 / 手工创建 / 其他（已废弃，删除） |
| transfer_type | 枚举 | 系统映射 | 调拨类型：国内→海外 / 海外→海外 / 退货返架 / FBA出库 |
| status | 枚举 | 系统 | 状态：待出库 / 已出库 / 在途 / 已签收 / 部分上架 / 已上架 / 已完成 / 已取消 |
| total_sku_count | 整数 | 系统映射 | SKU种数 |
| total_qty | 整数 | 系统映射 | 计划数量 |
| total_carton_count | 整数 | 系统统计 | 总箱数 |
| logistics_status | 字符串 | API/导入 | 物流状态（来自万邑通API或导入） |
| expected_arrival_date | 日期 | 系统计算 | 预计签收时间 = 提货时间 + 时效要求 |
| actual_arrival_date | 日期 | 导入 | 实际签收时间（已废弃，删除） |
| expected_shelf_date | 日期 | 系统计算 | 预计上架时间 = 预计签收时间 + 上架时效 |
| logistics_carrier | 字符串 | 导入 | 物流承运商 |
| logistics_tracking_no | 字符串 | 导入 | 物流单号 |
| is_customs_declared | 布尔 | 导入 | 是否报关 |
| customs_factory | 字符串 | 导入 | 报关工厂 |
| is_inspected | 布尔 | 导入 | 是否查验（海关实际查验，与"是否报关"不同） |
| timeline_requirement_days | 整数 | 导入 | 时效要求（天数），用于系统计算预计签收/上架时间 |
| order_remark | 文本 | 导入 | 订单备注 |
| transport_type | 枚举 | 导入 | 运输类型：海运 / 空运 / 铁路 / 卡航 |
| last_mile_type | 字符串 | 导入 | 尾程类型（已废弃，删除） |
| last_mile_channel | 字符串 | 导入 | 尾程渠道分类（如：快递派-GROUND、卡派-FTL） |
| pickup_time | 时间戳 | 导入 | 提货时间（收件日期，北京时间） |
| depart_time | 时间戳 | 导入 | 离港时间（主表汇总，取最早箱的离港时间） |
| arrive_port_time | 时间戳 | 导入 | 到港时间（主表汇总） |
| clearance_time | 时间戳 | 导入 | 清关时间（主表汇总） |
| last_mile_pickup_time | 时间戳 | 导入 | 尾程提取时间（主表汇总） |
| delivery_time | 时间戳 | 导入 | 签收时间（主表汇总） |
| unload_time | 时间戳 | API/导入 | 卸货时间（主表汇总） |
| shelve_time | 时间戳 | API/导入 | 上架时间（主表汇总） |
| is_logistics_abnormal | 布尔 | 系统判断/人工 | 是否物流异常 |
| logistics_abnormal_type | 字符串 | 系统判断 | 物流异常类型（规则可配置，如：超时未到港、超时未清关、超时未签收等） |
| logistics_abnormal_remark | 文本 | 导入 | 物流异常备注 |
| is_shelf_abnormal | 布尔 | 系统判断/人工 | 是否上架异常 |
| shelf_abnormal_type | 字符串 | 导入 | 上架异常类型 |
| shelf_abnormal_remark | 文本 | 导入 | 上架异常备注 |
| is_shelf_within_3days | 布尔 | 系统计算 | 是否3天内上架（已废弃，删除） |
| is_carton_within_11days | 布尔 | 系统计算 | 单箱是否11天内完成（已废弃，删除） |
| is_carton_within_7days | 布尔 | 系统计算 | 单箱是否7天内完成（已废弃，删除） |
| is_carton_within_4days | 布尔 | 系统计算 | 单箱是否4天内完成（已废弃，删除） |
| delay_explanation | 文本 | 导入 | 延迟说明 |
| estimated_unit_price | 小数 | 导入 | 预估单价（物流报价） |
| estimated_freight | 小数 | 系统计算 | 预估运费 = 预估单价 × 箱数（或按箱规计算） |
| total_freight_amount | 小数 | 导入/系统 | 最终运费（从运费账单汇总或手动录入） |
| freight_currency | 字符串 | 导入 | 运费币种（如：CNY、USD） |
| freight_allocation_method | 枚举 | 导入 | 运费分摊方式：按数量 / 按重量 / 按体积（默认按数量） |
| is_reconciled | 布尔 | 导入 | 是否已对账 |
| is_paid | 布尔 | 导入 | 是否已付款 |
| create_time | 时间戳 | 系统 | 系统创建时间（即导入/API创建时间） |
| update_time | 时间戳 | 系统 | 最近更新时间 |
| remark | 文本 | 导入 | 备注 |

> **字段来源说明**：
> - **系统映射**：从ERP/其他模块自动获取，调拨单创建时自动填充
> - **系统计算**：根据其他字段自动计算（如预计签收时间 = 提货时间 + 时效要求天数）
> - **系统判断**：根据可配置规则自动判断，也可人工覆盖
> - **API**：万邑通API自动抓取
> - **导入**：通过Excel/CSV导入或手动录入
>
> **关联键设计**：
> - `inbound_order_no`（第三方入库单号）是业务主键，所有导入操作以此匹配记录
> - `transfer_no`（调拨单号）是系统内部主键，用于表间关联
> - 两者一一对应，`inbound_order_no` 唯一约束确保不重复
>
> **物流节点双写设计**：
> - 主表（transfer_order）存汇总节点时间，取各箱中最早的时间，用于列表页展示和异常判断
> - 箱表（transfer_carton）存精确节点时间，用于详情页按箱展示和导出
> - 导入物流节点时，系统自动同步更新主表和箱表

### 3.2 调拨单明细表 — transfer_order_item

| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| id | 主键 | 系统 | 系统内部ID |
| transfer_no | 外键 | 系统 | 关联调拨单号 |
| inbound_order_no | 字符串 | 系统映射 | 第三方入库单号（冗余，便于按入库单号查询） |
| sku_code | 字符串 | 系统映射 | SKU编码 |
| sku_name | 字符串 | 系统映射 | SKU名称（从ERP导入） |
| expected_qty | 整数 | 系统映射 | 应调拨数量（来自ERP） |
| outbound_qty | 整数 | 导入 | 实际出库数量（出库回传时写入） |
| inbound_qty | 整数 | 导入 | 实际入库数量（入库回传时写入） |
| shelf_qty | 整数 | API/导入 | 上架数量（万邑通API或导入） |
| outbound_diff | 整数 | 系统计算 | 出库差异 = outbound_qty - expected_qty |
| inbound_diff | 整数 | 系统计算 | 入库差异 = inbound_qty - outbound_qty |
| total_diff | 整数 | 系统计算 | 总差异 = inbound_qty - expected_qty |
| diff_reason | 文本 | 导入 | 差异原因说明 |
| unit_weight | 小数 | 导入 | 单件重量（kg），用于按重量分摊运费 |
| unit_volume | 小数 | 导入 | 单件体积（m³），用于按体积分摊运费 |
| freight_cost_total | 小数 | 系统计算 | 该SKU分摊的总运费 = 总运费 × (本SKU占比) |
| freight_cost_per_unit | 小数 | 系统计算 | 单件运费成本 = freight_cost_total / outbound_qty |

### 3.3 在途轨迹表 — tracking_event

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| transfer_no | 外键 | 关联调拨单号 |
| event_time | 时间戳 | 事件发生时间 |
| event_type | 枚举 | 已发货 / 已到港口 / 清关中 / 清关完成 / 已提柜 / 派送中 / 已签收 / 异常 |
| event_desc | 文本 | 事件描述 |
| location | 字符串 | 当前位置 |
| operator | 字符串 | 录入人 |
| create_time | 时间戳 | 系统 | 系统记录时间 |

### 3.4 箱信息表 — transfer_carton

> 物流追踪和运费计算的核心粒度是**箱**，不是SKU。一个调拨单包含多个箱，每个箱包含多个SKU。

| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| id | 主键 | 系统 | 系统内部ID |
| transfer_no | 外键 | 系统 | 关联调拨单号 |
| inbound_order_no | 字符串 | 系统映射 | 第三方入库单号（核心关联键，导入时以此匹配） |
| carton_no | 字符串 | 导入/系统 | 箱号 |
| logistics_tracking_no | 字符串 | 导入 | 物流跟踪号（按箱） |
| logistics_carrier_order_no | 字符串 | 导入 | 物流商单号（已废弃，删除） |
| carton_length | 小数 | 导入 | 箱长（cm） |
| carton_width | 小数 | 导入 | 箱宽（cm） |
| carton_height | 小数 | 导入 | 箱高（cm） |
| carton_weight | 小数 | 导入 | 箱重（kg） |
| declared_value | 小数 | 导入 | 申报货值 |
| departure_time | 时间戳 | 导入 | 离港时间 |
| arrival_port_time | 时间戳 | 导入 | 到港时间 |
| customs_clearance_time | 时间戳 | 导入 | 清关时间 |
| last_mile_pickup_time | 时间戳 | 导入 | 尾程提取时间 |
| logistics_sign_time | 时间戳 | 导入 | 物流签收时间 |
| unload_time | 时间戳 | API/导入 | 卸货时间（万邑通API或导入） |
| shelf_time | 时间戳 | API/导入 | 上架时间（万邑通API或导入） |
| is_shelf_abnormal | 布尔 | 系统判断 | 是否上架异常（上架数量≠签收数量等） |
| shelf_abnormal_type | 字符串 | 导入 | 上架异常类型 |
| shelf_abnormal_remark | 文本 | 导入 | 上架异常备注 |
| receipt_time | 时间戳 | 导入 | 收件日期（已废弃，删除） |
| arrival_port_time_local | 时间戳 | 导入 | 到港日期（已废弃，删除） |
| departure_time_local | 时间戳 | 导入 | 起飞/离港日期（已废弃，删除） |
| landing_time_local | 时间戳 | 导入 | 落地日期（已废弃，删除） |
| last_mile_pickup_time_local | 时间戳 | 导入 | 提取日期（已废弃，删除） |
| logistics_sign_time_local | 时间戳 | 导入 | 签收日期（已废弃，删除） |
| unload_time_local | 时间戳 | API/导入 | 卸货时间（已废弃，删除） |
| shelf_time_local | 时间戳 | API/导入 | 上架时间（已废弃，删除） |
| checkout_to_sign_days | 小数 | 系统计算 | 签出-签收时效（已废弃，删除） |
| sign_to_shelf_days | 小数 | 系统计算 | 签收-上架时效（已废弃，删除） |
| unload_to_shelf_days | 小数 | 系统计算 | 卸货-上架时效（已废弃，删除） |
| is_shelf_within_3days | 布尔 | 系统计算 | 是否3天内上架（已废弃，删除） |
| is_carton_within_11days | 布尔 | 系统计算 | 单箱是否11天内（已废弃，删除） |
| is_carton_within_7days | 布尔 | 系统计算 | 单箱是否7天内（已废弃，删除） |
| is_carton_within_4days | 布尔 | 系统计算 | 单箱是否4天内（已废弃，删除） |
| create_time | 时间戳 | 系统 | 创建时间 |
| update_time | 时间戳 | 系统 | 更新时间 |

### 3.5 箱内SKU明细表 — transfer_carton_item

| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| id | 主键 | 系统 | 系统内部ID |
| carton_no | 外键 | 系统 | 关联箱号 |
| transfer_no | 外键 | 系统 | 关联调拨单号 |
| inbound_order_no | 字符串 | 系统映射 | 第三方入库单号 |
| sku_code | 字符串 | 系统映射 | SKU编码（系统内部） |
| sku_name | 字符串 | 系统映射 | SKU名称 |
| overseas_sku_code | 字符串 | 导入 | 海外仓SKU编码（与系统SKU可能不同） |
| product_name | 字符串 | 导入 | 品名（产品大类名，如：手机壳、充电器） |
| qty | 整数 | 系统映射 | 本箱内该SKU数量 |
| shelf_qty | 整数 | API/导入 | 本箱内该SKU上架数量 |

### 3.6 异常/差异处理表 — discrepancy_record

| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| id | 主键 | 系统 | 系统内部ID |
| transfer_no | 外键 | 系统 | 关联调拨单号 |
| carton_no | 字符串 | 系统 | 关联箱号（可选，箱级异常时填写） |
| sku_code | 字符串 | 系统 | 关联SKU |
| discrepancy_category | 枚举 | 系统 | 一级分类：数量差异 / 质量问题 / 物流异常 / 上架异常 |
| discrepancy_type | 枚举 | 系统 | 二级分类（见下方枚举定义） |
| discrepancy_qty | 整数 | 系统计算 | 差异数量 |
| status | 枚举 | 导入 | 待确认 / 处理中 / 已关闭 |
| handler | 字符串 | 导入 | 处理人 |
| resolution | 枚举 | 导入 | 处理结果（见下方关闭条件定义） |
| resolution_remark | 文本 | 导入 | 处理结果说明 |
| create_time | 时间戳 | 系统 | 记录时间 |
| update_time | 时间戳 | 系统 | 更新时间 |
| close_time | 时间戳 | 导入 | 关闭时间 |

> **异常分类枚举**：
>
> | 一级分类 (discrepancy_category) | 二级分类 (discrepancy_type) | 说明 |
> |---|---|---|
> | 数量差异 | 短装 | 实际数量 < 应发数量 |
> | | 溢装 | 实际数量 > 应发数量 |
> | | 错品 | SKU发错 |
> | 质量问题 | 破损 | 运输途中损坏 |
> | | 变质 | 产品变质/受潮 |
> | 物流异常 | 超时未到港 | 到港时间超过SLA |
> | | 超时未清关 | 清关时间超过SLA |
> | | 超时未签收 | 签收时间超过SLA |
> | | 丢失 | 货物丢失 |
> | 上架异常 | 部分上架 | 上架数量 < 签收数量 |
> | | 未上架 | 签收后超过上架SLA仍未上架 |
> | | 错位上架 | 上架到错误库位 |
>
> **关闭条件与处理结果 (resolution)**：
>
> | 异常类型 | 可选处理结果 | 关闭条件 |
> |---|---|---|
> | 短装 | 已理赔 / 已补发 / 已核销 | 选择任一处理结果后可关闭 |
> | 溢装 | 已核销 / 已退回 | 选择任一处理结果后可关闭 |
> | 错品 | 已换货 / 已核销 | 选择任一处理结果后可关闭 |
> | 破损 | 已理赔 / 已退货 | 选择任一处理结果后可关闭 |
> | 变质 | 已理赔 / 已退货 / 已销毁 | 选择任一处理结果后可关闭 |
> | 超时类 | 已到下一节点 / 延迟说明已填写 | 物流已推进到下一节点，或填写延迟说明后可关闭 |
> | 丢失 | 已理赔 / 已找到 | 选择任一处理结果后可关闭 |
> | 部分上架 | 已补上架 / 已理赔 / 已核销 | 选择任一处理结果后可关闭 |
> | 未上架 | 已上架 | 上架数据已回传后可关闭 |
> | 错位上架 | 已调整 | 库位调整完成后可关闭 |
>
> **升级策略**：
> - Phase 1：异常超过7天未关闭 → 列表页标红提醒
> - Phase 3：升级为可配置升级规则（如：3天未处理提醒处理人，7天未关闭升级给团队负责人）

### 3.7 运费账单表 — freight_bill

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| bill_no | 字符串(唯一) | 账单编号（如：FB-20260523-0001） |
| transfer_no | 外键 | 关联调拨单号 |
| logistics_carrier | 字符串 | 物流商名称 |
| freight_fee | 小数 | 运费 |
| customs_fee | 小数 | 清关费 |
| other_fee | 小数 | 其他费用 |
| total_amount | 小数 | 账单总金额 = freight_fee + customs_fee + other_fee |
| currency | 字符串 | 币种（CNY / USD / EUR） |
| exchange_rate | 小数 | 汇率（对CNY），用于统一核算 |
| total_amount_cny | 小数 | 折合人民币金额 = total_amount × exchange_rate |
| bill_date | 日期 | 账单日期 |
| bill_status | 枚举 | 待确认 / 已确认 / 已对账 |
| confirm_time | 时间戳 | 确认时间 |
| confirmer | 字符串 | 确认人 |
| remark | 文本 | 备注 |
| create_time | 时间戳 | 创建时间 |
| update_time | 时间戳 | 更新时间 |

> **运费分摊逻辑**：当运费账单确认后，系统自动将 total_amount_cny 按调拨单的 freight_allocation_method 分摊到每个SKU：
> - **按数量**：SKU分摊额 = 总运费 × (SKU的outbound_qty / 所有SKU的outbound_qty之和)
> - **按重量**：SKU分摊额 = 总运费 × (SKU的outbound_qty × unit_weight / 所有SKU的重量之和)
> - **按体积**：SKU分摊额 = 总运费 × (SKU的outbound_qty × unit_volume / 所有SKU的体积之和)
>
> **精度规则**：分摊金额保留2位小数，除不尽时尾差归最后一个SKU（确保所有SKU分摊额之和 = 总运费）。例如：总运费100元，3个SKU各占1/3，则前两个各33.33元，最后一个33.34元。
>
> 分摊后自动写入 transfer_order_item 的 freight_cost_total 和 freight_cost_per_unit，同时更新 transfer_order 的 total_freight_amount。
>
> 一笔调拨单可关联多笔运费账单（如分段运输），系统汇总后统一分摊。

### 3.9 变更日志表 — change_log

> 每次数据更新均保留变更记录，不可删除，保留完整审计轨迹。Phase 1先写入数据库表，Phase 3可在详情页展示操作历史。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | 系统内部ID |
| record_type | 枚举 | 记录类型：transfer_order / transfer_carton / transfer_order_item |
| record_id | 整数 | 关联记录ID（调拨单ID/箱ID/SKU明细ID） |
| transfer_no | 字符串 | 关联调拨单号（冗余，便于按单查询） |
| field_name | 字符串 | 变更字段名 |
| old_value | 文本 | 旧值 |
| new_value | 文本 | 新值 |
| change_source | 枚举 | 变更来源：API / 导入 / 手动录入 |
| operator | 字符串 | 操作人（API自动时为"系统"） |
| change_time | 时间戳 | 变更时间 |
| reason | 文本 | 变更原因（手动覆盖时必填） |

> **查询场景**：调拨单详情页的"操作历史"Tab，按 transfer_no 查询，按 change_time 倒序排列。

## 四、状态流转图

```
ERP导入 / API创建（万邑通）
   │
   ▼
┌───────┐   出库回传导入    ┌───────┐
│ 待出库 │ ────────────▶ │ 已出库 │
└───────┘                └───────┘
   │                         ▲
   └── 手动确认出库 ──────────┘
                            │
                       物流信息导入/录入
                       （提货时间、物流商、物流单号）
                            ▼
                        ┌──────┐
                        │ 在途  │ ◀──── 物流节点导入/录入
                        └──────┘      （离港/到港/清关/尾程提取）
                            │
                       物流签收时间导入/录入
                            ▼
                        ┌──────┐
                        │ 已签收│
                        └──────┘
                            │
                       上架信息（万邑通API / 导入/录入）
                       ┌──┴──┐
                       ▼     ▼
              ┌──────────┐  ┌──────┐
              │ 部分上架  │  │ 已上架│
              └──────────┘  └──────┘
                   │            │
                   └──▶ 已上架 ─┘
                                │
                       运费对账完成
                            ▼
                        ┌──────┐
                        │ 已完成 │
                        └──────┘

各节点均可流向【已取消】
各节点如有差异，生成差异记录（不影响主流程）
在途/已签收节点如有异常，标记物流异常（导入或手动）
已签收/已上架节点如有异常，标记上架异常（导入或手动）
所有状态变更均可通过导入或手动操作触发
```

### 状态变更规则

- 系统不自动流转状态，所有状态变更由导入或人工操作触发
- 出库回传非必须，支持手动确认出库（点击[确认出库]按钮）
- 每个状态变更应有操作人记录
- 状态变更后，系统自动校验并发出提醒（如差异超过阈值时标红）
- 所有字段均支持导入和手动录入双轨操作

#### 状态变更触发条件

| 当前状态 | 目标状态 | 触发条件 | 操作方式 |
|---|---|---|---|
| 待出库 | 已出库 | 导入出库回传（至少1个SKU有outbound_qty），或手动确认出库 | 导入 / 手动 |
| 已出库 | 在途 | 导入物流信息（有提货时间pickup_time或物流单号logistics_tracking_no） | 导入 / 手动 |
| 在途 | 已签收 | 导入签收时间（delivery_time） | 导入 / 手动 |
| 已签收 | 部分上架 | 万邑通API回传部分上架数据，或手动确认部分上架（部分SKU有shelf_qty但未全部上架） | API / 导入 / 手动 |
| 已签收 | 已上架 | 万邑通API回传上架数据，或手动录入上架数据（至少1个箱有shelf_time） | API / 导入 / 手动 |
| 部分上架 | 已上架 | 所有SKU上架完成 | API / 导入 / 手动 |
| 部分上架 | 已完成 | 运费对账完成（is_reconciled = true） | 导入 / 手动 |
| 已上架 | 已完成 | 运费对账完成（is_reconciled = true） | 导入 / 手动 |
| 任意 | 已取消 | 手动操作 | 手动 |

> **注意**：
> - 状态只能向前流转（待出库→已出库→在途→已签收→已上架→已完成），不可逆
> - 已取消是终态，不可恢复
> - 差异记录和异常标记不影响主流程状态流转
> - 部分出库/部分签收时，只要有至少1个SKU/箱有数据即触发状态变更

---

## 四、关键设计决策

> 本章集中记录系统架构层面的关键决策点及其技术动机，方便后续开发人员理解"为什么这样设计"。

### 4.1 关联键设计：双主键策略

| 决策 | 选择 | 原因 | 备选方案 |
|---|---|---|---|
| 业务主键 | `inbound_order_no`（第三方入库单号） | 业务人员日常沟通以入库单号为标识，所有导入操作以此匹配 | 只用transfer_no（业务人员不认识系统单号） |
| 系统主键 | `transfer_no`（调拨单号） | 系统内部表间关联需要稳定、可控的自生成主键 | 用inbound_order_no做系统主键（依赖第三方格式，风险高） |

> 两者一一对应，`inbound_order_no` 唯一约束确保不重复。业务侧用入库单号，系统侧用调拨单号，各取所需。

### 4.2 规则引擎：先简后繁

| 决策 | 选择 | 原因 | 备选方案 |
|---|---|---|---|
| Phase 1 | 简单表 `sla_rules`，按目的仓+运输类型配置SLA天数 | MVP阶段规则少（仅SLA天数和上架时效），简单表2小时能做完 | JSON规则引擎（开发周期长，MVP阶段杀鸡用牛刀） |
| Phase 3 | 升级为 `rule_config`，支持异常判断/超时预警/多条件组合/优先级 | 业务规则随运营成熟而复杂化，需要可配置能力 | 继续硬编码规则（每次改规则要改代码，不可持续） |

### 4.3 物流节点存储：双写设计

| 决策 | 选择 | 原因 | 备选方案 |
|---|---|---|---|
| 主表（transfer_order） | 存汇总节点时间，取各箱中最早的时间 | 列表页需要快速展示每单的物流进度，不能每次聚合查询 | 只存箱表，列表页聚合查询（数据量大时性能差） |
| 箱表（transfer_carton） | 存精确节点时间 | 详情页需要按箱展示物流轨迹，导出也需要箱级粒度 | 只存主表（丢失箱级精度，无法按箱追踪） |

> 导入物流节点时，系统自动同步更新主表和箱表。主表取值规则：取同调拨单下所有箱中该节点的最早时间。

### 4.4 团队/用户表：提前到Phase 1

| 决策 | 选择 | 原因 | 备选方案 |
|---|---|---|---|
| 团队表 | Phase 1 | 调拨单主表有team字段，数据归属需要团队维度 | Phase 4再做（Phase 1数据无团队归属，后续补填成本高） |
| 用户表 | Phase 1基础版（登录+角色） | 系统需要登录鉴权，操作日志需要记录操作人 | Phase 4再做（Phase 1无登录保护，数据安全无保障） |

> Phase 1用户表只做基础：登录/登出 + 三种角色（ADMIN/OPERATOR/WAREHOUSE）。Phase 4细化：字段级权限、操作审批、数据范围权限。

### 4.5 导入模板：标准格式优先

| 决策 | 选择 | 原因 | 备选方案 |
|---|---|---|---|
| 模板策略 | 系统仅支持标准模板格式导入 | 兼容非标准格式是无限深渊，每个仓库格式不同，维护成本极高 | 支持多格式自动识别（开发成本高，易出错） |
| 旧数据迁移 | 一次性脚本转换后导入 | 旧格式数据量有限，一次性处理即可 | 系统内置格式转换（增加系统复杂度） |

> 项目启动前需先收集各仓库、各物流商的实际Excel/CSV样本，确认标准模板的可行性，必要时调整标准模板字段以兼容常见格式。

### 4.6 运费模型：两阶段设计

| 决策 | 选择 | 原因 | 备选方案 |
|---|---|---|---|
| 预估阶段 | 出货后1-2天登记预估单价，系统自动计算预估运费 | 财务需要预估运费做预算，不能等最终账单 | 只有最终运费（预算阶段无数据） |
| 最终阶段 | 物流商寄账单后导入，确认后自动分摊到SKU | per-SKU运费成本是财务硬需求 | 只到调拨单级运费（无法满足SKU级成本核算） |

---

## 五、导入模板设计

### 5.1 调拨单导入模板（主导入）

最核心的导入，一次性导入调拨单全部基础信息。**以第三方入库单号为主匹配键，粒度为"箱×SKU"**。

> 实际业务中，创建调拨单时发货仓、目的仓、物流商、报关、箱规等信息基本同时就有，因此合并为一次导入，无需分步。

**文件名规范**：调拨单_YYYYMMDD_HHMMSS.xlsx

| inbound_order_no | transfer_no | outbound_order_no | source | from_warehouse | to_warehouse | team | product_name | transport_type | logistics_carrier | logistics_carrier_order_no | carton_count | expected_qty | carton_spec | is_customs_declared | customs_factory | timeline_requirement_days | sku_code | overseas_sku_code | sku_qty | carton_no | remark |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| RK-20260523-001 | TO-20260523-0001 | CK-001 | 万邑通 | 广州仓 | 洛杉矶仓 | 北美组 | 手机壳 | 海运 | 中通国际 | WY20260523001 | 2 | 500 | 60×40×40 | 是 | 广州报关行 | 15 | A123 | MU7576552360-30 | 30 | 1 | |
| RK-20260523-001 | TO-20260523-0001 | CK-001 | 万邑通 | 广州仓 | 洛杉矶仓 | 北美组 | 手机壳 | 海运 | 中通国际 | WY20260523001 | 2 | 500 | 60×40×40 | 是 | 广州报关行 | 15 | B456 | MU7576552360-20 | 20 | 1 | |
| RK-20260523-001 | TO-20260523-0001 | CK-001 | 万邑通 | 广州仓 | 洛杉矶仓 | 北美组 | 充电器 | 海运 | 中通国际 | WY20260523001 | 2 | 300 | 60×40×40 | 是 | 广州报关行 | 15 | C789 | MU7576552360-10 | 10 | 2 | |
| RK-20260523-002 | TO-20260523-0002 | CK-002 | 手工创建 | 深圳仓 | FBA-US | 北美组 | 数据线 | 空运 | DHL | DHL20260523002 | 1 | 1000 | 50×30×30 | 否 | | 8 | D012 | MU7576552361-50 | 50 | 1 | 旺季补货 |

> **导入规则**：
> - 每行代表一个箱内的一个SKU（箱×SKU粒度）
> - 系统根据 `inbound_order_no` 自动归并成一个调拨单
> - 同一 `inbound_order_no` + `carton_no` 的行归入同一个箱
> - `carton_spec`（箱规）格式：长×宽×高（cm），同一箱号只取第一行的值
> - `timeline_requirement_days` 为整数天数，系统据此计算预计签收时间
> - `source`（来源）标识数据来源：万邑通API / 亚马逊 / 手工创建 / 其他
> - `overseas_sku_code` 为海外仓自己的SKU编码，与系统SKU可能不同
> - 导入后系统自动创建：调拨单主记录 + 箱信息记录 + 箱内SKU明细记录

### 5.2 出库回传模板

国内仓出库后回传的数据。**按第三方入库单号匹配**。

| inbound_order_no | sku_code | outbound_qty | outbound_date | remark |
|---|---|---|---|---|
| RK-20260523-001 | A123 | 498 | 2026-05-25 | 2件质检不合格退回 |
| RK-20260523-001 | B456 | 300 | 2026-05-25 | |

> **导入后行为**：系统根据 `inbound_order_no` 找到对应调拨单，更新 outbound_qty，自动计算 outbound_diff，diff != 0 则生成差异记录，状态变为「已出库」。

### 5.3 物流信息导入模板

**粗粒度——关联物流信息**（按第三方入库单号匹配）：

| inbound_order_no | logistics_carrier | logistics_tracking_no | expected_arrival_date |
|---|---|---|---|
| RK-20260523-001 | 中通国际 | LT123456 | 2026-06-10 |

**细粒度——在途轨迹**（按第三方入库单号匹配）：

| inbound_order_no | event_time | event_type | event_desc | location |
|---|---|---|---|---|
| RK-20260523-001 | 2026-05-25 14:00 | 已发货 | 货物已从广州仓发出 | 广州 |
| RK-20260523-001 | 2026-05-27 09:00 | 已到港口 | 货柜抵达上海港 | 上海港 |
| RK-20260523-001 | 2026-05-28 16:00 | 清关中 | 提交清关资料 | 上海港 |
| RK-20260523-001 | 2026-06-02 10:00 | 清关完成 | 清关放行 | 上海港 |
| RK-20260523-001 | 2026-06-08 14:00 | 派送中 | 派送中 | 洛杉矶 |
| RK-20260523-001 | 2026-06-10 11:00 | 已签收 | 仓库已签收 | 洛杉矶仓 |

> event_time 默认 UTC+8，建议在文件名或备注中标明时区。

### 5.4 入库回传模板

海外仓入库后回传的数据。**按第三方入库单号匹配**。

| inbound_order_no | sku_code | inbound_qty | inbound_date | diff_reason |
|---|---|---|---|---|
| RK-20260523-001 | A123 | 496 | 2026-06-10 | 运输破损2件，已理赔 |
| RK-20260523-001 | B456 | 300 | 2026-06-10 | |

> **导入后行为**：系统根据 `inbound_order_no` 找到对应调拨单，更新 inbound_qty，自动计算 inbound_diff 和 total_diff，diff != 0 生成差异记录，状态变为「已完成」。

### 5.5 报关与物流信息补充导入

> 当5.1主导入时报关/物流信息不全时，可按第三方入库单号补充导入。**5.1已包含这些字段，本模板仅在补充/修正时使用。**

| inbound_order_no | is_customs_declared | customs_factory | timeline_requirement_days | order_remark | transport_type | logistics_carrier | logistics_carrier_order_no | pickup_time | is_inspected | last_mile_type | last_mile_channel |
|---|---|---|---|---|---|---|---|---|---|---|---|
| RK-20260523-001 | 是 | 广州报关行 | 15 | 旺季补货 | 海运 | 中通国际 | WY20260523001 | 2026-05-25 10:00 | 否 | 快递派 | 快递派-GROUND |

> **导入后行为**：更新调拨单的报关、物流、时效字段。系统自动计算预计签收时间（= 提货时间 + 时效要求天数）和预计上架时间。

### 5.6 箱规与申报货值补充导入

> 当5.1主导入时箱规/申报货值不全时，可按第三方入库单号补充导入。**5.1已包含箱规字段，本模板仅在补充/修正时使用。**

| inbound_order_no | carton_no | carton_length | carton_width | carton_height | carton_weight | declared_value |
|---|---|---|---|---|---|---|
| RK-20260523-001 | CTN001 | 60 | 40 | 40 | 15.5 | 5000 |
| RK-20260523-001 | CTN002 | 60 | 40 | 40 | 16.0 | 5200 |

> **导入后行为**：创建/更新箱信息记录。配合预估单价，系统自动计算预估运费。

### 5.7 预估单价导入模板

物流报价获取后，及时登记预估单价。

| inbound_order_no | estimated_unit_price | freight_currency |
|---|---|---|
| RK-20260523-001 | 1200 | CNY |
| RK-20260523-002 | 350 | USD |

> **导入后行为**：更新预估单价，系统自动计算预估运费 = 预估单价 × 箱数。

### 5.8 物流跟踪号导入模板

提货后2天内，按箱号回传物流跟踪号。

| inbound_order_no | carton_no | logistics_tracking_no |
|---|---|---|
| RK-20260523-001 | CTN001 | LT123456789 |
| RK-20260523-001 | CTN002 | LT123456790 |

### 5.9 物流时间节点导入模板

周一/三/五按物流商单号、第三方入库单号批量导入。

| inbound_order_no | carton_no | departure_time | arrival_port_time | customs_clearance_time | last_mile_pickup_time | logistics_sign_time |
|---|---|---|---|---|---|---|
| RK-20260523-001 | CTN001 | 2026-05-28 | 2026-06-05 | 2026-06-07 | 2026-06-08 | 2026-06-10 |
| RK-20260523-001 | CTN002 | 2026-05-28 | 2026-06-05 | 2026-06-07 | 2026-06-08 | 2026-06-10 |

> **导入后行为**：更新箱的物流时间节点。系统自动判断物流异常（根据可配置规则，如：超过时效要求未签收、到港后超时未清关等），更新 is_logistics_abnormal 和 logistics_abnormal_type。

### 5.10 物流异常备注导入模板

筛选物流异常调拨单，核实后导入备注。

| inbound_order_no | logistics_abnormal_remark |
|---|---|
| RK-20260523-001 | 船期延误，预计延迟3天 |

### 5.11 上架异常导入模板

人工核实上架异常后导入。

| inbound_order_no | carton_no | shelf_abnormal_type | shelf_abnormal_remark |
|---|---|---|---|
| RK-20260523-001 | CTN001 | 部分上架 | 2件破损未上架，已联系海外仓处理 |

### 5.12 运费账单导入模板

物流商寄来运费账单后，导入系统关联到调拨单。

| inbound_order_no | logistics_carrier | freight_fee | customs_fee | other_fee | currency | exchange_rate | bill_date | remark |
|---|---|---|---|---|---|---|---|---|
| RK-20260523-001 | 中通国际 | 15000 | 2000 | 500 | CNY | 1 | 2026-05-25 | |
| RK-20260523-002 | DHL | 800 | 0 | 0 | USD | 7.25 | 2026-05-26 | 加急件 |

> **导入后行为**：自动计算 total_amount 和 total_amount_cny，账单状态默认为「待确认」。确认后触发运费分摊，自动计算每个SKU的 freight_cost_total 和 freight_cost_per_unit。

### 5.13 运费对账导入模板

每月核对上月调拨运费，确认对账和付款状态。

| inbound_order_no | is_reconciled | is_paid | remark |
|---|---|---|---|
| RK-20260523-001 | 是 | 是 | |
| RK-20260523-002 | 是 | 否 | 待6月底付款 |

### 5.14 导入通用规则

- 支持 Excel (.xlsx) 和 CSV (.csv) 两种格式
- **系统仅支持标准模板格式导入，不兼容非标准格式**。各仓库/物流商须按标准模板填写，旧格式数据通过一次性脚本转换后导入
- **所有导入模板以 `inbound_order_no`（第三方入库单号）为主匹配键**，系统根据此字段匹配到对应调拨单后更新数据
- 导入时自动做字段校验（必填项、格式、数值范围）
- 导入结果反馈：成功几条、失败几条、失败原因逐行提示
- 所有导入操作记录日志，可追溯
- **数据冲突处理**：当导入数据与已有数据冲突时，按第六章「数据冲突解决策略」处理

#### 5.14.1 各模板必填字段

| 模板 | 必填字段 |
|---|---|
| 5.1 主导入 | inbound_order_no, from_warehouse, to_warehouse, transport_type, carton_no, sku_code, sku_qty |
| 5.2 出库回传 | inbound_order_no, sku_code, outbound_qty |
| 5.3 物流信息（粗粒度） | inbound_order_no |
| 5.3 物流信息（细粒度） | inbound_order_no, event_time, event_type |
| 5.4 入库回传 | inbound_order_no, sku_code, inbound_qty |
| 5.5 报关补充 | inbound_order_no |
| 5.6 箱规补充 | inbound_order_no, carton_no |
| 5.7 预估单价 | inbound_order_no, estimated_unit_price |
| 5.8 物流跟踪号 | inbound_order_no, carton_no, logistics_tracking_no |
| 5.9 物流时间节点 | inbound_order_no, carton_no |
| 5.10 物流异常备注 | inbound_order_no |
| 5.11 上架异常 | inbound_order_no |
| 5.12 运费账单 | inbound_order_no, freight_fee, currency |
| 5.13 运费对账 | inbound_order_no |

> 必填字段缺失时，该行导入失败并在结果中提示具体原因。非必填字段缺失时不报错，保留原值（新增时为空）。

#### 5.14.2 导入增量/覆盖策略

| 模板 | 默认策略 | 说明 |
|---|---|---|
| 5.1 主导入 | 覆盖 | 同入库单号再次导入时，覆盖已有数据（以最新导入为准） |
| 5.2 出库回传 | 覆盖 | 同入库单号+SKU再次导入时，覆盖该SKU的出库数量 |
| 5.3 物流信息（粗粒度） | 覆盖 | 同入库单号再次导入时，覆盖物流商/物流单号等字段 |
| 5.3 物流信息（细粒度） | **追加** | 同入库单号可追加多个物流事件，不覆盖已有事件 |
| 5.4 入库回传 | 覆盖 | 同入库单号+SKU再次导入时，覆盖该SKU的入库数量 |
| 5.5-5.13 补充导入 | 覆盖 | 同入库单号再次导入时，覆盖对应字段 |

> **追加模式**仅用于物流轨迹（5.3细粒度），因为同一调拨单可以有多个物流事件。其余模板均为覆盖模式。

### 5.15 导入与录入双轨原则

> **所有支持导入的字段，均须同时支持在页面上手动录入/编辑。** 导入是批量操作的高效方式，手动录入是单条操作的灵活方式，两者互为补充。

- 调拨单详情页的每个区域均提供[编辑]按钮，支持逐字段手动修改
- 手动录入与导入写入同一数据源，无差异
- 出库回传非必须：可在详情页手动点击[确认出库]将状态从"待出库"推进到"已出库"，无需导入出库数据
- 异常信息、报关信息、物流信息等均可通过导入或手动录入两种方式维护
- 所有手动操作同样记录操作日志

---

## 六、数据冲突解决策略

> 系统存在三种数据写入路径：API自动抓取（万邑通）、Excel/CSV导入、手动录入。当同一字段被不同来源写入时，需要明确的冲突解决规则。

### 6.1 来源优先级

当同一字段被多个来源同时写入时，按以下优先级处理：

| 优先级 | 来源 | 说明 |
|---|---|---|
| 1（最高） | API自动抓取 | 万邑通等第三方API返回的数据，时效性和准确性最高 |
| 2 | 导入数据 | Excel/CSV批量导入，经过人工整理 |
| 3（最低） | 手动录入 | 单条手动编辑，灵活性最高但可靠性最低 |

> **例外**：人工手动修改始终可以覆盖任何来源的数据，但必须记录覆盖原因。

### 6.2 差异阈值与自动挂起

当新数据与已有数据的差异超过阈值时，系统不自动覆盖，而是挂起待人工确认：

| 字段类型 | 阈值 | 挂起行为 |
|---|---|---|
| 数量类（outbound_qty、inbound_qty、shelf_qty） | 差异 > 5% 或 > 5件 | 不自动覆盖，标记为「待确认」，列表页标黄提醒 |
| 金额类（estimated_unit_price、total_freight_amount） | 差异 > 10% | 不自动覆盖，标记为「待确认」 |
| 时间类（各物流节点时间） | 新值与旧值差 > 3天 | 不自动覆盖，标记为「待确认」 |
| 其他字段 | 无阈值 | 按来源优先级覆盖 |

> 差异在阈值内的，按来源优先级自动覆盖。

### 6.3 变更日志

每次数据更新均保留变更记录：

| 字段 | 说明 |
|---|---|
| record_id | 关联记录ID（调拨单/箱/SKU） |
| field_name | 变更字段名 |
| old_value | 旧值 |
| new_value | 新值 |
| change_source | 变更来源：API / 导入 / 手动录入 |
| operator | 操作人（API自动时为"系统"） |
| change_time | 变更时间 |
| reason | 变更原因（手动覆盖时必填） |

> 变更日志不可删除，保留完整审计轨迹。Phase 1先写入数据库表，Phase 3可在详情页展示操作历史。

---

## 七、页面结构设计

### 7.1 页面清单

| 页面 | 核心功能 |
|---|---|
| 调拨看板（Dashboard） | 全局概览：各状态调拨单数量 + 在途SKU汇总 + 异常预警 + 预估运费汇总 |
| 调拨单列表 | 搜索/筛选/排序，列表展示所有调拨单（API创建和导入创建统一呈现） |
| 调拨单详情 | SKU明细 + 箱明细 + 在途轨迹 + 差异记录 + 运费信息 + 操作历史 |
| 在途追踪 | 在途调拨单列表视图，按预计签收时间排序，支持导出在途明细给计划部门 |
| 导出中心 | 全局导出任务管理面板，显示所有导出任务的进度、状态，支持即时反馈和自动下载 |

#### 在途明细导出规格

> 在途数据是计划部门的核心参考，需要定期（周一/三/五）导出给其他团队查看。字段设计参考现有在途数据导出格式。

**导出入口**：在途追踪页面 → [导出在途明细] 按钮

**筛选条件**：状态=在途（可叠加：发货仓、目的仓、业务团队、物流商、是否异常、运输类型）

**导出字段**：

| 字段 | 说明 |
|---|---|
| 第三方入库单号 | 核心关联键 |
| 调拨单号 | 系统内部单号 |
| 入库单+箱号 | 复合唯一标识（入库单号+箱号） |
| 箱号 | |
| 系统SKU | 系统内部SKU编码 |
| 海外仓SKU | 海外仓SKU编码 |
| 计划数量 | |
| 实际发货数量 | |
| 上架数量 | 实际上架数量 |
| 状态 | 当前物流状态 |
| 上架情况 | 系统自动判断：已上架 / 未上架 / 部分上架 |
| 发货仓 | |
| 目的仓 | |
| 团队 | |
| 运输类型 | 海运/空运/铁路/卡航 |
| 运输时效要求(天) | |
| 时效是否达标 | 系统自动判断：出库-到仓时效是否≤时效要求 |
| 运单号 | 物流跟踪号 |
| 发货日期 | 提货时间（收件日期） |
| 离港时间 | |
| 到港时间 | |
| 清关时间 | |
| 尾程提取时间 | |
| 到仓日期 | 物流签收时间 |
| 卸货时间 | |
| 上架时间 | |
| 出库-到仓时效(天) | 系统计算 = 到仓日期 - 离港时间 |
| 出库-上架时效(天) | 系统计算 = 上架时间 - 离港时间 |
| 卸货-上架时效(天) | 系统计算 = 上架时间 - 卸货时间 |
| 预计上架时间 | 系统计算 |
| 上架数量差异 | 上架数量 - 实际发货数量 |
| 是否物流异常 | |
| 物流异常备注 | |
| 延迟说明 | |
| 是否查验 | 海关查验 |
| 尾程渠道分类 | |
| 发货日期年份 | 发货日期的年份 |
| 发货日期月份 | 发货日期的月份 |

> **上架情况自动判断规则**：
> - **已上架**：所有SKU的shelf_qty ≥ outbound_qty（或expected_qty）
> - **未上架**：所有SKU的shelf_qty = 0
> - **部分上架**：部分SKU已上架但未全部上架（即非"已上架"也非"未上架"）

**导出格式**：Excel (.xlsx)，文件名规范：在途明细_YYYYMMDD.xlsx

**附加Sheet**：
- **异常汇总**：列出所有物流异常和上架异常的调拨单及说明
- **SLA统计**：各路线的SLA达标率（3天内上架率、7天内完成率等）

#### 导出中心

> 解决导出速度慢、点击无反馈的问题。用户点击导出后立即得到响应，可在导出中心面板中实时查看进度，完成后自动下载。

**入口位置**：全局 Header 右侧，用户名左侧

**交互流程**：

```
1. 用户点击[导出]按钮
2. 系统立即创建异步导出任务（毫秒级响应）
3. 导出中心面板自动打开，显示新任务
4. 后台生成Excel文件，前端每秒轮询进度
5. 进度条实时更新（10%→20%→40%→60%→70%→85%→95%）
6. 完成后自动触发文件下载
7. 用户可随时关闭面板，任务继续在后台执行
```

**导出中心面板功能**：

| 功能 | 说明 |
|---|---|
| 任务列表 | 显示所有导出任务（最新的在前），包含文件名、状态、进度 |
| 进度条 | 进行中的任务显示进度条和百分比 |
| 状态图标 | ✅已完成 / ❌失败 / 🔄进行中 |
| 下载按钮 | 已完成的任务可点击重新下载 |
| 删除按钮 | 可删除任意任务记录 |
| 红点Badge | Header按钮上显示活跃任务数量 |
| 自动关闭 | 点击面板外部自动关闭 |

**任务状态**：

| 状态 | 说明 |
|---|---|
| pending | 任务已创建，等待处理 |
| processing | 后台正在生成Excel文件 |
| completed | 文件生成完毕，自动触发下载 |
| failed | 生成失败，显示错误信息 |

**技术架构**：异步任务模式 — POST创建任务 → GET轮询状态 → GET下载文件（详见11.10节）
| 导入管理 | 各类模板上传入口 + 导入历史查看 |
| 物流异常管理 | 物流异常调拨单筛选 + 异常类型统计 + 异常备注导入 |
| 上架异常管理 | 上架异常调拨单筛选 + 异常核实 + 异常备注导入 |
| 差异处理 | 所有差异记录列表 + 处理状态管理 |
| 运费账单 | 运费账单列表 + 确认/对账 + 预估运费 vs 最终运费 + SKU运费成本查看 |
| 仓库管理 | 仓库列表 + 新增/编辑/启停 + API对接配置 |
| 物流商管理 | 物流商列表 + 新增/编辑/启停 + 支持路线/运输类型配置 |
| 团队管理 | 团队列表 + 新增/编辑/启停 |
| 用户管理 | 用户列表 + 新增/编辑 + 角色分配 + 启停 |
| SLA规则配置 | 按目的仓+运输类型配置SLA天数 + 上架SLA天数 |
| API对接管理 | API列表 + 连接测试 + 同步日志 + 手动触发同步 |
| 报表中心 | 在途库存报表、时效分析、差异分析、SKU运费成本报表、FBA专报 |

### 7.2 核心页面原型描述

#### 调拨看板（Dashboard）

```
┌──────────────────────────────────────────────────────┐
│  调拨看板                                   [导入]   │
├──────────────────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 待出库│ │ 已出库│ │ 在途  │ │ 已完成│ │ 异常  │       │
│  │  12   │ │  8   │ │  15  │ │  89  │ │  3   │       │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘       │
├──────────────────────────────────────────────────────┤
│  在途库存汇总（按SKU）                                │
│  ┌─────────┬────────┬────────┬────────┬─────────┐    │
│  │ SKU     │ 在途总数 │ 调拨单数 │ 最早到仓 │ 最迟到仓 │    │
│  ├─────────┼────────┼────────┼────────┼─────────┤    │
│  │ A123    │ 1,500  │  3     │ 06-01  │ 06-15   │    │
│  │ B456    │ 800    │  2     │ 06-03  │ 06-10   │    │
│  │ C789    │ 2,000  │  4     │ 05-28  │ 06-12   │    │
│  └─────────┴────────┴────────┴────────┴─────────┘    │
├──────────────────────────────────────────────────────┤
│  最新异常提醒                                          │
│  TO-20260523-0005 入库差异 -5件（A123） 待处理        │
│  TO-20260523-0003 在途超时（预计05-20到达，已超3天）   │
└──────────────────────────────────────────────────────┘
```

#### 调拨单详情页

```
调拨单 TO-20260523-0001
┌──────────────────────────────────────────────────────┐
│ 状态：在途     广州仓 → 洛杉矶仓    团队：北美组      │
│ 出库单：CK-001  入库单：RK-001                        │
│ 物流：中通国际 WY20260523001  提货：05-25 10:00       │
│ 时效要求：15天  预计签收：06-09  预计上架：06-12       │
│ 运输类型：海运  报关：是（广州报关行）                  │
├──────────────────────────────────────────────────────┤
│  SKU明细                                              │
│  ┌────────┬──────┬──────┬──────┬──────┬──────┬────┐  │
│  │ SKU    │ 应发  │ 实发  │ 出库差│ 实收  │ 上架 │总差│  │
│  ├────────┼──────┼──────┼──────┼──────┼──────┼────┤  │
│  │ A123   │ 500  │ 498  │ -2   │  -   │  -   │ -  │  │
│  │ B456   │ 300  │ 300  │  0   │  -   │  -   │ -  │  │
│  └────────┴──────┴──────┴──────┴──────┴──────┴────┘  │
├──────────────────────────────────────────────────────┤
│  箱明细                                               │
│  ┌────────┬──────────┬──────┬──────┬──────┐          │
│  │ 箱号   │ 物流跟踪号 │ 重量  │ 申报值│ 状态  │          │
│  ├────────┼──────────┼──────┼──────┼──────┤          │
│  │ CTN001 │ LT123456 │ 15.5 │ 5000 │ 在途  │          │
│  │ CTN002 │ LT123457 │ 16.0 │ 5200 │ 在途  │          │
│  └────────┴──────────┴──────┴──────┴──────┘          │
│  [导入箱规]  [导入物流跟踪号]  [导入物流节点]           │
├──────────────────────────────────────────────────────┤
│  物流节点（按箱）                                      │
│  CTN001: 05-28 离港 → 06-05 到港 → 06-07 清关 → ...  │
│  CTN002: 05-28 离港 → 06-05 到港 → 06-07 清关 → ...  │
├──────────────────────────────────────────────────────┤
│  异常状态                                              │
│  物流异常：否    上架异常：-                           │
├──────────────────────────────────────────────────────┤
│  差异记录                                              │
│  出库差异：A123 短少2件（质检退回） 已确认              │
├──────────────────────────────────────────────────────┤
│  运费信息                                              │
│  预估单价：¥1,200/箱  预估运费：¥2,400                 │
│  最终运费：-                                          │
│  对账状态：-    付款状态：-                            │
│  [导入预估单价]  [导入运费账单]  [导入对账状态]         │
│                                                        │
│  SKU运费成本（账单确认后自动计算）                       │
│  ┌────────┬──────┬──────┬────────────┬──────────────┐  │
│  │ SKU    │ 数量  │ 总运费│ 单件运费成本│ 分摊方式     │  │
│  ├────────┼──────┼──────┼────────────┼──────────────┤  │
│  │ A123   │ 498  │¥1,196│ ¥2.40     │ 按数量分摊   │  │
│  │ B456   │ 300  │ ¥1,204│ ¥4.01     │ 按数量分摊   │  │
│  └────────┴──────┴──────┴────────────┴──────────────┘  │
├──────────────────────────────────────────────────────┤
│  操作历史                                              │
│  05-23 14:00 系统导入（ERP：PO-20260523-001）          │
│  05-25 16:00 导入出库回传                              │
│  05-25 16:00 状态变更：待出库 → 已出库                 │
│  05-25 17:00 导入报关与物流信息                        │
│  05-25 17:00 状态变更：已出库 → 在途                   │
│  05-26 10:00 导入箱规与申报货值                        │
│  05-26 11:00 导入预估单价，预估运费：¥2,400            │
│  05-27 09:00 导入物流跟踪号                            │
└──────────────────────────────────────────────────────┘
```

---

## 八、报表设计

### 8.1 在途库存报表

用途：掌握当前在途的货物总量，按SKU汇总，支持采购/销售决策。

| SKU | 在途总数 | 涉及调拨单数 | 调拨单明细 | 最早预计到仓 | 最迟预计到仓 |
|---|---|---|---|---|---|
| A123 | 1,500 | 3 | TO-001(500), TO-003(800), TO-007(200) | 2026-06-01 | 2026-06-15 |

导出格式：Excel，列同上。

### 8.2 调拨时效分析报表

用途：评估各仓之间调拨耗时，发现效率瓶颈。

| 路线 | 平均天数 | 出库→到港 | 清关 | 到港→入库 | 样本数 |
|---|---|---|---|---|---|
| 广州→洛杉矶 | 14 | 3天 | 5天 | 6天 | 25 |
| 深圳→FBA-US | 8 | 2天 | 3天 | 3天 | 18 |
| 义乌→东京 | 10 | 4天 | 4天 | 2天 | 10 |

### 8.3 差异分析报表

用途：找出高差异率的SKU和仓库，针对性改善。

| SKU | 调拨次数 | 总出库差异 | 总入库差异 | 差异率 | 主要差异原因 |
|---|---|---|---|---|---|
| A123 | 10 | -15 | -8 | 2.3% | 质检退回、运输破损 |

| 仓库 | 出库差异率 | 主要问题 |
|---|---|---|
| 广州仓 | 1.2% | 漏发 |
| 义乌仓 | 3.5% | 错发、包装破损 |

### 8.4 SKU运费成本报表

用途：财务需要的核心报表，提供每个SKU的运费成本数据，用于财务报表和成本核算。

**按SKU汇总**：

| SKU | SKU名称 | 调拨次数 | 总运费(CNY) | 总数量 | 平均单件运费 | 期间 |
|---|---|---|---|---|---|---|
| A123 | 手机壳 | 10 | ¥109,130 | 4,980 | ¥21.91 | 2026-05 |
| B456 | 充电器 | 8 | ¥52,696 | 2,400 | ¥21.96 | 2026-05 |

**按路线+SKU汇总**：

| 路线 | SKU | 调拨次数 | 总运费(CNY) | 平均单件运费 |
|---|---|---|---|---|
| 广州→洛杉矶 | A123 | 6 | ¥65,478 | ¥22.00 |
| 深圳→FBA-US | A123 | 4 | ¥43,652 | ¥21.75 |

导出格式：Excel，支持按月/季度/自定义时间范围筛选。

### 8.5 FBA调拨专项报表

用途：单独看所有发往FBA的调拨，方便和亚马逊后台对账。

| 调拨单 | SKU | 应发 | 实发 | FBA收货 | 差异 | 状态 |

---

## 九、业务操作节奏

> 本章节定义了系统在实际业务中的操作节奏，确保系统设计贴合日常使用场景。

| 频率 | 操作 | 数据来源 | 说明 |
|---|---|---|---|
| **实时** | 调拨单自动呈现 | 系统映射/API | ERP导入或万邑通API创建后自动呈现 |
| **每天（出货后）** | 导入报关信息、物流商、提货时间 | 5.5 报关与物流信息导入 | 货发出后按第三方入库单号导入 |
| **出货后1-2天** | 导入箱规、申报货值、预估单价 | 5.6 箱规导入 + 5.7 预估单价导入 | 获取报价后及时登记，系统自动计算预估运费 |
| **提货后2天内** | 导入物流跟踪号（按箱） | 5.8 物流跟踪号导入 | 按箱号回传运单号 |
| **周一/三/五** | 批量导入物流时间节点 | 5.9 物流时间节点导入 | 按物流商单号、第三方入库单号批量导入 |
| **导入后自动** | 系统判断物流异常 | 系统自动 | 根据可配置规则自动判断，也可人工标记 |
| **不定期** | 导入物流异常备注 | 5.10 物流异常备注导入 | 核实后导入 |
| **每天上午** | API抓取万邑通上架情况 | 万邑通API | 按箱获取卸货时间、上架时间、上架数量 |
| **不定期** | 导入上架异常信息 | 5.11 上架异常导入 | 人工核实后导入异常类型和备注 |
| **周一/三/五** | 导出在途明细给计划部门 | 在途追踪页面导出 | 筛选在途中，导出{调拨单-箱号-SKU明细-预计签收时间-物流异常备注&上架异常备注} |
| **每月** | 核对上月调拨运费 | 5.12 运费账单 + 5.13 运费对账导入 | 确认对账、付款状态 |

---

## 十、MVP分期规划

### Phase 1 — 核心闭环（目标：跑通数据通路）

预估工期：4-6周（2名开发）

| 功能 | 优先级 | 说明 |
|---|---|---|
| 用户管理（基础） | P0 | 登录/登出 + 三种角色（ADMIN/OPERATOR/WAREHOUSE）+ 用户CRUD |
| 仓库管理 | P0 | 仓库CRUD + 启停 + 时区配置 |
| 物流商管理 | P0 | 物流商CRUD + 启停 + 支持路线配置 |
| 团队管理 | P0 | 团队CRUD + 启停 |
| SLA规则配置 | P0 | 按目的仓+运输类型配置SLA天数 |
| ERP调拨单导入 | P0 | 导入模板 + 校验 + 结果反馈 |
| 出库回传导入 | P0 | 导入后自动计算差异 |
| 入库回传导入 | P0 | 导入后自动计算差异，完成闭环 |
| 调拨单列表 + 详情页 | P0 | 查看所有调拨单及SKU明细（API创建和导入创建统一呈现） |
| 报关与物流信息导入 | P0 | 导入报关、物流商、提货时间、时效要求等 |
| 箱规与申报货值导入 | P0 | 导入箱信息，支持按箱追踪 |
| 状态手动变更 | P0 | 人工确认状态变化（含手动出库） |

Phase 1 不做：万邑通API对接、异常判断自动触发、运费分摊、自动报表、差异处理工作流、FBA专报、API对接管理页面、可配置规则引擎。

### Phase 2 — 在途强化与运费管理（目标：精度提升+费用可控）

| 功能 | 优先级 | 说明 |
|---|---|---|
| 物流跟踪号导入（按箱） | P1 | 提货后按箱号导入物流跟踪号 |
| 物流时间节点导入 | P1 | 周一/三/五批量导入离港/到港/清关等节点 |
| 物流异常自动判断 | P1 | 根据可配置规则自动判断物流异常+异常类型，也可人工标记 |
| 物流异常备注导入 | P1 | 核实后导入异常备注 |
| 在途看板 | P1 | 在途调拨汇总看板 |
| SKU级在途查询 | P1 | 按SKU搜索在途货物 |
| 超时预警 | P1 | 超过预计签收时间未签收自动标红 |
| 在途明细导出 | P1 | 筛选在途中的，导出给计划部门 |
| 预估单价导入 | P1 | 出货后1-2天登记预估单价 |
| 预估运费自动计算 | P1 | 预估单价 × 箱数，自动计算 |
| 运费账单导入 | P1 | 导入物流商运费账单，关联调拨单 |
| 运费自动分摊 | P1 | 账单确认后自动分摊到SKU，计算单件运费成本 |
| SKU运费成本查看 | P1 | 调拨单详情页展示per-SKU运费成本 |
| 运费对账状态导入 | P1 | 每月核对上月运费，确认对账和付款状态 |

### Phase 3 — 上架追踪与差异报表

| 功能 | 优先级 | 说明 |
|---|---|---|
| 万邑通API对接 | P2 | 每天自动抓取上架情况（卸货时间、上架时间、上架数量） |
| API对接管理页面 | P2 | API列表 + 连接测试 + 同步日志 + 手动触发同步 |
| 可配置规则引擎 | P2 | 升级sla_rules为rule_config，支持异常判断/超时预警/多条件组合/优先级 |
| 上架异常自动判断 | P2 | 系统判断上架异常，人工核实确认 |
| 上架异常备注导入 | P2 | 人工核实后导入异常类型和备注 |
| 差异处理工作流 | P2 | 差异确认、指派、关闭流程 |
| 在途库存报表 | P2 | SKU维度汇总导出 |
| 时效分析报表 | P2 | 路线平均耗时统计 |
| 差异分析报表 | P2 | SKU/仓库维度差异率统计 |
| SKU运费成本报表 | P2 | 按SKU/路线汇总运费成本，支持财务导出 |
| FBA专项功能 | P2 | FBA调拨单独标记和报表 |

### Phase 4 — 扩展能力

| 功能 | 优先级 | 说明 |
|---|---|---|
| 用户权限细化 | P3 | 字段级权限、操作审批权限、数据范围权限（按团队/仓库） |
| 通知提醒 | P3 | 异常推送（邮件/企微/钉钉） |
| 数据看板大屏 | P3 | 大屏展示在途/差异/运费 |

---

## 十一、技术方案

### 11.1 技术栈

```
┌─────────────────────────────────────────────────┐
│                   前端 (Vite)                      │
│  React 18 + TypeScript + Tailwind CSS            │
│  Zustand + React Router v7 + lucide-react        │
├─────────────────────────────────────────────────┤
│                   后端 (Hono)                     │
│  TypeScript + Zod 校验                           │
│  node-cron + job_queue 表 (定时任务)              │
│  xlsx (Excel) + axios (万邑通 API)                │
│  JWT + bcrypt + RBAC 权限                        │
├─────────────────────────────────────────────────┤
│               数据库层 (knex.js)                   │
│  SQLite (better-sqlite3) — WAL 模式               │
│  knex migrations + 参数化查询                     │
│  开发即生产数据库，零运维                          │
├─────────────────────────────────────────────────┤
│              共享目录 (shared/)                   │
│  Zod Schemas → TypeScript 类型推导                 │
│  状态枚举 + 常量 + API 路径常量                   │
└─────────────────────────────────────────────────┘
```

**选型理由**：

| 技术 | 为什么选 |
|---|---|
| Hono | TypeScript 一等公民，内置 Zod 校验中间件，API 输入/输出类型一次生成 |
| knex.js | 迁移脚本（改表有记录）+ 参数化查询（防注入），不是 ORM |
| SQLite | 年增 1-2GB，10-20 人并发，WAL 模式绰绰有余，不计划迁移 PG |
| Zod | 一套 Schema → 前端校验 + 后端校验 + TypeScript 类型，三合一 |
| 无 ORM | 数据量不大，直接 knex + SQL 更可控，AI 生成质量更高 |

### 11.2 数据量评估

| 表 | 月增 | 年增 | SQLite 压力 |
|---|---|---|---|
| transfer_order | ~1,000 | ~12,000 | 🟢 |
| transfer_carton | ~15,000 | ~180,000 | 🟢 |
| transfer_carton_item | ~100,000 | ~1,200,000 | 🟢 |
| tracking_event | ~20,000 | ~240,000 | 🟢 |
| discrepancy_record | ~1,000 | ~12,000 | 🟢 |
| freight_bill | ~800 | ~9,600 | 🟢 |
| change_log | ~50,000 | ~600,000 | 🟢 |
| **合计** | **~200,000** | **~2,350,000** | 🟢 5-10 年无压力 |

> **结论**：SQLite 不需要迁移 PostgreSQL。knex 的迁移工具仅仅是为了团队协作和改表结构有记录，不是为了切换数据库。

### 11.3 项目目录结构

```
transfer-management/
├── client/                        # 前端
│   ├── src/
│   │   ├── api/                   # API 调用（类型安全，由 zod schema 推导）
│   │   ├── components/            # 通用组件（Tailwind 自定义）
│   │   ├── pages/                 # 页面
│   │   │   ├── dashboard/         # 仪表盘
│   │   │   ├── orders/            # 调拨单列表 + 详情
│   │   │   ├── import/            # 数据导入
│   │   │   ├── warehouse/         # 仓库管理
│   │   │   ├── carrier/           # 物流商管理
│   │   │   ├── tracking/          # 在途追踪
│   │   │   ├── freight/           # 运费管理
│   │   │   ├── discrepancy/       # 差异管理
│   │   │   ├── reports/           # 报表
│   │   │   ├── settings/          # 系统设置
│   │   │   └── auth/              # 登录
│   │   ├── store/                 # Zustand
│   │   ├── hooks/                 # 自定义 hooks
│   │   └── App.tsx
│   ├── index.html
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── package.json
│
├── server/                        # 后端
│   ├── src/
│   │   ├── routes/                # Hono 路由
│   │   ├── db/                    # knex 实例 + 查询函数
│   │   ├── migrations/            # knex 迁移脚本
│   │   ├── middleware/            # 认证 / 错误处理 / 日志
│   │   ├── services/              # 业务逻辑
│   │   ├── cron/                  # 定时任务
│   │   ├── jobs/                  # job_queue 任务执行器
│   │   └── index.ts               # 入口
│   ├── knexfile.ts                # knex 配置（SQLite）
│   ├── tsconfig.json
│   └── package.json
│
├── shared/                        # 前后端共享
│   ├── schemas/                   # Zod schemas
│   ├── types/                     # 推导出的 TypeScript 类型
│   └── constants/                 # 枚举 + 常量
│
├── data/                          # SQLite 数据库文件（gitignore）
├── docker-compose.yml
└── README.md
```

### 11.4 Phase 1 开发顺序

> 按依赖关系排列，每个步骤产出可测试的功能。认证放在导入之后，优先验证核心数据通路。

| 步 | 内容 | 产出物 | 验证方式 |
|---|---|---|---|
| 1 | 项目脚手架 | Vite + Hono + knex + SQLite 初始化 | npm run dev 两端启动成功 |
| 2 | 数据库 Schema | 所有建表迁移脚本 + 种子数据 | knex migrate:latest 执行成功 |
| 3 | 后端 API 骨架 | 路由挂载 + 中间件（错误/日志） | curl 测试 /api/health |
| 4 | 基础 CRUD | 仓库 / 物流商 / 团队 / 用户 API | 增删改查测试 |
| 5 | 调拨单导入 | Excel 解析 + 校验 + 写入 | 导入一个真实 Excel 文件 |
| 6 | 认证 | JWT 登录 + RBAC 中间件 | 登录 → 访问受保护接口 |
| 7 | 调拨单列表 | 列表页（筛选/排序/分页） | 浏览器查看列表 |
| 8 | 调拨单详情 | 详情页（Tab 式：箱级/物流/差异/日志） | 点击查看详情 |
| 9 | 出库/入库回传 | 导入 + 差异计算 + 状态流转 | 导入回传文件验证状态变化 |
| 10 | 在途看板 + 超时预警 | 在途汇总 + SLA 规则判断 | 查看在途数据，验证超时标记 |
| 11 | 在途明细导出 | 导出 Excel（按数据真实字段） | 导出文件内容验证 |
| 12 | 联调 + 修 bug | 全流程走通 | 完整业务场景测试 |

**AI 开发注意事项**：

| 原则 | 说明 |
|---|---|
| 单任务粒度 | 一个 prompt 只做一个功能（如"生成仓库管理 CRUD API + 前端页面"） |
| 先 API 后页面 | 后端 API 确定后再生成前端页面，避免前后端对不上 |
| 每个功能立即测试 | 生成完就手动验证，不堆积未测试代码 |
| 同会话开发 | 尽量在一个 AI 会话中连续开发，保持上下文 |

### 11.5 部署方案

**Phase 1（开发验证）**：

```
开发机
├── npm run dev（前后端同时启动）
├── data/transfer.db（SQLite 本地文件）
└── localhost:5173（前端）/ localhost:3001（API）
```

**Phase 2（内网/云部署）**：

```
Docker 容器
├── 一个容器就够了
├── Hono 服务（内嵌前端静态文件）
├── SQLite 文件挂载 volume
├── docker-compose.yml 一键启动
└── 内网 IP 访问
```

> SQLite 的备份就是 `cp data/transfer.db backup/` 一句话的事。

**部署策略总结**：

| 阶段 | 方式 | 复杂度 |
|---|---|---|
| 开发验证 | npm run dev | 🟢 零配置 |
| 内网试用 | Docker 单容器 | 🟢 一个 compose 文件 |
| 正式使用 | Docker + 定时备份 | 🟢 运维成本极低 |

### 11.6 权限模型

> Phase 1 建表时就建好 RBAC 结构，但 UI 不做复杂权限管理。Phase 1 只做页面级权限（有权限才看到菜单），字段级 + 审批流留到 Phase 4 再实现，数据库结构不用改。

```
users          — 用户表（Phase 1 实现 CRUD）
  ├── role_id → roles
  └── team_id → teams

roles          — 角色表（预设：管理员 / 运营 / 仓库 / 只读）
  └── id ← role_permissions.role_id

role_permissions — 角色权限（permission_code 如：order.import, freight.view）
```

**Phase 1 预置权限码**（约15-20个）：

| 模块 | 权限码 | 说明 |
|---|---|---|
| 调拨单 | order.view | 查看调拨单列表和详情 |
| | order.import | 导入调拨单 |
| | order.edit | 编辑调拨单（手动录入/修改） |
| | order.confirm | 确认出库/签收等状态变更 |
| 在途 | tracking.view | 查看在途追踪 |
| | tracking.export | 导出在途明细 |
| 运费 | freight.view | 查看运费信息 |
| | freight.confirm | 确认运费账单 |
| | freight.reconcile | 运费对账/付款 |
| 异常 | discrepancy.view | 查看异常记录 |
| | discrepancy.handle | 处理异常（确认/关闭） |
| 报表 | reports.view | 查看报表 |
| 设置 | settings.manage | 仓库/物流商/团队/SLA/用户管理 |
| 导入 | import.execute | 执行数据导入 |

### 11.7 定时任务方案

```
node-cron（定时触发）
    │
    ▼
job_queue 表（记录每次执行）
  ├── job_type: 'winit_sync' / 'sla_check' 等
  ├── status: 'pending' | 'running' | 'success' | 'failed'
  ├── started_at / finished_at
  ├── result (JSON) / error (text)
  └── retry_count ≤ 2
```

- 服务器重启时检查 `status = 'running'` 的 job，重置为 pending
- 手动触发入口（API）：`POST /api/jobs/trigger?type=winit_sync`
- 失败重试最多 2 次，超过后标记失败并记录错误信息

### 11.8 报表性能预留

> 调拨单→箱→SKU三层数据结构在报表场景下行数会快速膨胀（一个柜可能几百个SKU × 多个箱子 × 多个时间节点）。Phase 1建表时需提前考虑汇总视图，避免Phase 2-3再做数据重构。

**预留汇总视图**（Phase 1建空壳SQL VIEW，Phase 2-3填充实际查询逻辑）：

| 视图名 | 用途 | Phase |
|---|---|---|
| `v_intransit_summary` | 在途汇总：按SKU+目的仓聚合在途数量 | Phase 2 |
| `v_freight_cost_by_sku` | SKU运费成本：按SKU聚合运费（分摊后） | Phase 2 |
| `v_sla_compliance` | SLA达标：按路线统计达标率（3天内上架率、7天内完成率等） | Phase 3 |
| `v_discrepancy_summary` | 差异汇总：按SKU/仓库聚合差异率 | Phase 3 |

> **性能升级路径**：如果数据量增长到百万级（约1-2年后），将SQL VIEW替换为物化视图或定时刷新的汇总表（如每小时刷新一次）。Phase 1-2阶段数据量在万级，SQL VIEW性能足够。

### 11.9 不做的事（边界确认）

### 11.10 异步导出架构

> 导出操作（在途明细、调拨单列表等）涉及大量数据查询和Excel生成，同步导出会导致用户等待时间长且无法感知进度。采用异步任务模式解决此问题。

**架构模式**：

```
┌──────────┐    POST /export     ┌──────────────────┐
│  前端页面  │ ──────────────────▶ │  后端创建异步任务   │
│          │ ◀────────────────── │  立即返回 taskId   │
└──────────┘                     └──────────────────┘
     │                                    │
     │  GET /export/:taskId/status        │ 后台异步生成Excel
     │  （每秒轮询）                        │ updateProgress(10→95)
     │                                    │
     │                                    ▼
     │                           ┌──────────────────┐
     │  GET /export/:taskId/download │  completeTask(buf) │
     │ ────────────────────────── │  任务完成，存储buffer │
     │ ◀────────────────────────  └──────────────────┘
     │
     ▼
  自动下载文件
```

**后端组件**：

| 组件 | 文件 | 说明 |
|---|---|---|
| ExportTaskManager | server/src/services/exportTaskManager.ts | 内存级任务管理器，Map存储任务状态和文件buffer |

**ExportTaskManager 核心接口**：

| 方法 | 说明 |
|---|---|
| createTask(type, fileName) | 创建任务，返回任务对象（含唯一taskId） |
| getTask(taskId) | 查询任务状态 |
| updateProgress(taskId, progress) | 更新进度 |
| completeTask(taskId, buffer) | 标记完成，存储Excel文件buffer |
| failTask(taskId, error) | 标记失败 |
| cleanOldTasks() | 清理30分钟前的已完成/失败任务 |

**任务数据结构**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 字符串 | 唯一任务ID |
| type | 字符串 | 导出类型（tracking / orders） |
| fileName | 字符串 | 下载文件名 |
| status | 枚举 | pending / processing / completed / failed |
| progress | 整数 | 当前进度值 |
| total | 整数 | 总进度值（默认100） |
| buffer | Buffer/null | Excel文件二进制数据 |
| error | 字符串/null | 失败原因 |
| createdAt | 数字 | 创建时间戳 |

**API路由设计**（以在途明细为例）：

| 路由 | 方法 | 说明 |
|---|---|---|
| /tracking/export | POST | 创建异步导出任务，立即返回 `{ taskId }` |
| /tracking/export/:taskId/status | GET | 查询任务进度，返回 `{ status, progress, total, error }` |
| /tracking/export/:taskId/download | GET | 下载已完成的Excel文件 |

**前端组件**：

| 组件 | 文件 | 说明 |
|---|---|---|
| exportStore | client/src/store/exportStore.ts | Zustand store，管理任务列表、轮询、自动下载 |
| ExportCenter | client/src/components/ExportCenter.tsx | 导出中心面板组件，进度条+状态图标+下载/删除 |

**前端轮询策略**：

- 创建任务后立即开始轮询，间隔1秒
- 状态为 completed 时停止轮询并自动下载
- 状态为 failed 时停止轮询并显示错误
- 网络错误时停止轮询并标记失败

**限制与注意事项**：

- 任务存储在内存中，服务器重启后任务丢失
- 已完成任务的文件buffer保留30分钟后自动清理
- 适用于中小规模导出（万级数据量），超大规模导出需升级为文件系统存储

| 不做 | 原因 |
|---|---|
| 不替代 ERP 创建调拨单 | 本系统只做导入和追踪 |
| 不替代物流商系统 | 不做 WMS/TMS |
| 不接 FBA API | 通过导入管理 |
| 不接非万邑通仓库 API | 通过导入管理 |
| 不做消息队列（RabbitMQ/Kafka） | 10-20 人，node-cron + job_queue 表足够 |
| 不计划迁 PostgreSQL | SQLite 5-10 年无压力 |

---

## 十二、文档更新日志

| 日期 | 版本 | 变更内容 | 作者 |
|---|---|---|---|
| 2026-05-23 | v0.1 | 初版草案 | Codex |
| 2026-05-23 | v0.2 | 补充仓库主表、运费账单表、运费分摊逻辑、SKU运费成本报表 | SOLO |
| 2026-05-23 | v0.3 | 整合业务需求：增加箱级追踪、报关管理、物流/上架异常判断、预估运费、API对接万邑通、操作节奏、运费对账 | SOLO |
| 2026-05-23 | v0.4 | 第三方入库单号作为核心关联键贯穿全系统；所有导入模板改用inbound_order_no匹配；增加在途明细导出规格 | SOLO |
| 2026-05-23 | v0.5 | 合并5.1为主导入（箱×SKU粒度）；新增双SKU体系、时区标注、SLA达标判断、尾程信息、时效统计、是否查验；在途导出按真实数据字段重写 | SOLO |
| 2026-05-23 | v0.6 | 导入与录入双轨原则；出库回传非必须支持手动出库；所有字段支持导入+手动录入 | SOLO |
| 2026-05-23 | v0.7 | 新增仓库管理（含时区/API配置）、物流商管理、规则配置（SLA/超时/异常）、API对接配置；页面和MVP分期同步更新 | SOLO |
| 2026-05-23 | v0.8 | 吸收现有代码经验：规则引擎简化为sla_rules（Phase 3再升级）、物流节点双写（主表汇总+箱表精确）、新增团队表和用户表（Phase 1基础模块）、用户管理从Phase 4提前到Phase 1 | SOLO |
| 2026-05-23 | v0.9 | 新增数据冲突解决策略（来源优先级+差异阈值+变更日志）；细化异常分类枚举（4大类12子类）和关闭条件；新增关键设计决策章节（6个决策点）；补充报表性能预留（4个汇总视图）；导入通用规则增加标准格式约束和数据冲突处理引用 | SOLO |
| 2026-05-24 | v1.0 | 确定技术方案：Hono+knex+SQLite+Zod技术栈；用户权限从枚举role升级为RBAC三表模型（roles+role_permissions+users）；新增job_queue定时任务队列表；新增完整技术方案章节（技术栈/数据量评估/目录结构/开发顺序/部署方案/权限模型/定时任务/边界确认）；Phase 1开发顺序调整（认证和导入对调，先验证核心数据通路）；状态从草案升级为确认 | SOLO |
| 2026-05-24 | v1.1 | 代码生成就绪度补齐：补充各导入模板必填字段清单（5.14.1）；补充导入增量/覆盖默认策略（5.14.2，仅物流轨迹追加，其余覆盖）；补充状态变更触发条件精确化（部分出库/签收即触发）；补充运费分摊精度规则（保留2位小数，尾差归最后一个SKU）；新增change_log变更日志表（3.9节，从6.3节升级为正式数据模型） | SOLO |
| 2026-05-31 | v1.2 | 系统审计与功能增强：(1) 新增「部分上架」状态（PARTIAL_SHELVED），状态流转图增加已签收→部分上架→已上架分支；(2) 在途明细导出字段更新：新增上架数量、上架情况、时效是否达标、到仓日期、出库-到仓时效、出库-上架时效、卸货-上架时效、发货日期年份/月份；删除是否3天内上架、单箱是否11/7/4天内、尾程等旧字段；上架情况自动判断（已上架/未上架/部分上架）；(3) 废弃字段清理：transfer_orders表删除erp_order_no、source、actual_arrival_date、last_mile_type、is_shelf_within_3days、is_carton_within_11/7/4days共8个字段；transfer_cartons表删除logistics_carrier_order_no、receipt_time、各local时间字段、checkout_to_sign_days、sign_to_shelf_days、is_shelf_within_3days、is_carton_within_11/7/4days共17个字段；(4) 新增异步导出中心：ExportTaskManager内存级任务管理器+三路由异步模式（POST创建/GET轮询/GET下载）+前端ExportCenter面板（进度条+状态图标+自动下载）+Zustand exportStore | SOLO |

---

> **接下来可以做的事**：
> 1. 收集各仓库、各物流商的实际Excel/CSV样本，确认标准模板可行性
> 2. 按第11.4节开发顺序，从项目脚手架开始逐步实现Phase 1
> 3. 每个步骤完成后立即测试验证，不堆积未测试代码
> 4. Phase 1完成后，用真实数据做全流程验证
